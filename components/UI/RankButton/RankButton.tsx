
export default function RankButton({rank=0}:{rank: number}) {
    return (
        <div>
            <button></button>
            <span>{rank}</span>
            <button></button>
        </div>
    );
}